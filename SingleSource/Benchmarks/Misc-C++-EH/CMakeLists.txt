# Autogenerated from SingleSource/Benchmarks/Misc-C++-EH/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../..)
list(APPEND LDFLAGS -lm -lstdc++)
set(LIBS -lstdc++)
set(REQUIRES_EH_SUPPORT 1)
llvm_singlesource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
