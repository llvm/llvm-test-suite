# Autogenerated from SingleSource/Benchmarks/Adobe-C++/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../..)
list(APPEND LDFLAGS -lm -lstdc++)
set(LIBS -lstdc++)
set(FP_ABSTOLERANCE 0.01)
llvm_singlesource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
