# Autogenerated from MultiSource/Benchmarks/TSVC/InductionVariable-dbl/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG InductionVariable-dbl)
set(LDFLAGS -lm)
list(APPEND CFLAGS -std=gnu99)
set(RUN_OPTIONS 9100 14)
if("${ARCH}" STREQUAL "XCore")
  set(RUNTIMELIMIT 7200)
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
