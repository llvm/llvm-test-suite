set(PROG enc-md5)
if(DEFINED LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 50)
else()
  if(DEFINED SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 1)
  else()
    set(RUN_OPTIONS 10)
  endif()
endif()
llvm_multisource()
