# Autogenerated from MultiSource/Benchmarks/Trimaran/enc-md5/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG enc-md5)
if(DEFINED LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 50)
else()
  if(DEFINED SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 1)
  else()
    set(RUN_OPTIONS 10)
  endif()
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
