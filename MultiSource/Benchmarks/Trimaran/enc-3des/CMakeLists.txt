set(PROG enc-3des)
if(DEFINED LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 200)
else()
  if(DEFINED SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 10)
  else()
    set(RUN_OPTIONS 100)
  endif()
endif()
llvm_multisource()
