list(APPEND CFLAGS -Wno-implicit-int)
if(LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 34)
else()
  if(SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 4)
  else()
    set(RUN_OPTIONS 16)
  endif()
endif()
llvm_multisource(ecbdes)
set_property(TARGET ecbdes PROPERTY C_STANDARD 17)
