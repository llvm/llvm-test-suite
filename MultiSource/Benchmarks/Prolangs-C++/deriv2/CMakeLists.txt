# Autogenerated from MultiSource/Benchmarks/Prolangs-C++/deriv2/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG deriv2)
set(LDFLAGS -lstdc++)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
