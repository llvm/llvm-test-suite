# Autogenerated from MultiSource/Benchmarks/Prolangs-C++/trees/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG trees)
set(LIBS -lstdc++)
set(LDFLAGS -lstdc++)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
