# Autogenerated from MultiSource/Benchmarks/Prolangs-C/plot2fig/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
if(DEFINED HAVE_RE_COMP)
  set(PROG plot2fig)
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
