# Autogenerated from MultiSource/Benchmarks/Prolangs-C/assembler/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG assembler)
set(LDFLAGS -lm)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
