# Autogenerated from MultiSource/Benchmarks/Prolangs-C/archie-client/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
if(DEFINED HAVE_RE_COMP)
  set(PROG archie)
endif()
if("${TARGET_OS}" STREQUAL "SunOS")
  list(APPEND LDFLAGS -lsocket -lnsl)
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
