# Autogenerated from MultiSource/Benchmarks/Prolangs-C/bison/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG mybison)
set(RUN_OPTIONS ${CMAKE_CURRENT_SOURCE_DIR}/parse.y.in -v)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
