# Autogenerated from MultiSource/Benchmarks/Prolangs-C/cdecl/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
list(APPEND CFLAGS -std=gnu89)
set(PROG cdecl)
set(STDIN_FILENAME ${CMAKE_CURRENT_SOURCE_DIR}/testset)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
