# Autogenerated from MultiSource/Benchmarks/Prolangs-C/unix-smail/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG unix-smail)
set(RUN_OPTIONS -v ALL)
set(STDIN_FILENAME ${CMAKE_CURRENT_SOURCE_DIR}/main.c)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
