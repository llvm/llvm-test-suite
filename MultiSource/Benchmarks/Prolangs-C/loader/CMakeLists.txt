# Autogenerated from MultiSource/Benchmarks/Prolangs-C/loader/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(LDFLAGS -lm)
set(PROG loader)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
