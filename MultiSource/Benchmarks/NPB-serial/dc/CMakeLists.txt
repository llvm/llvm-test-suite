# Autogenerated from MultiSource/Benchmarks/NPB-serial/dc/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG dc)
set(LDFLAGS )
set(Source adc.c dc.c jobcntl.c c_print_results.c extbuild.c rbt.c)
set(RUN_OPTIONS 10000000 ADC.par)
if(DEFINED SMALL_PROBLEM_SIZE)
  set(CPPFLAGS -DSMALL_PROBLEM_SIZE)
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
