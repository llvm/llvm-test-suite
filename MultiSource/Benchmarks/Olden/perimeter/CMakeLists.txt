list(APPEND CPPFLAGS -DTORONTO)
if(LARGE_PROBLEM_SIZE)
  set(RUN_OPTIONS 11)
else()
  if(SMALL_PROBLEM_SIZE)
    set(RUN_OPTIONS 9)
  else()
    set(RUN_OPTIONS 10)
  endif()
endif()
llvm_multisource(perimeter)
