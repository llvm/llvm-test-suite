# Autogenerated from MultiSource/Benchmarks/ASC_Sequoia/sphot/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../../..)
set(PROG sphot)
list(APPEND LIBS -lm)
list(APPEND LDFLAGS -lm)
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
