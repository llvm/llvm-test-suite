# Autogenerated from MultiSource/Applications/sgefa/Makefile

set(DIRS)
set(PARALLEL_DIRS)

set(LEVEL ../../..)
set(PROG sgefa)
set(LDFLAGS -lm)
set(FP_TOLERANCE 0.0001)
if(DEFINED LARGE_PROBLEM_SIZE)
  set(CPPFLAGS -DSCALE=50)
else()
  set(CPPFLAGS -DSCALE=40)
endif()
llvm_multisource()

llvm_add_subdirectories(${DIRS} ${PARALLEL_DIRS})
